[pytest]
minversion = 8.0
testpaths = tests
pythonpath = .
addopts = -q
markers =
    unit: fast isolated unit tests
    integration: api integration tests with dependency overrides
    e2e: end-to-end tests
    slow: long-running tests
